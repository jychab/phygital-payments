{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter phygital-wallet-sdk build",
    "dev": "wrangler dev",
    "predeploy": "pnpm --filter phygital-wallet-sdk build",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types --env-interface Env ./worker-env.d.ts",
    "pretypecheck": "pnpm --filter phygital-wallet-sdk build",
    "typecheck": "tsc --noEmit",
    "pretest": "pnpm --filter phygital-wallet-sdk build",
    "test": "vitest run"
  },
  "dependencies": {
    "@noble/curves": "^2.3.0",
    "@solana-program/system": "^0.14.0",
    "@solana-program/token": "^0.16.0",
    "@solana/kit": "^8.1.0",
    "helius-sdk": "3.1.0",
    "hono": "^4.9.0",
    "phygital-token-sdk": "^1.0.3",
    "phygital-wallet-sdk": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250726.0",
    "typescript": "^5",
    "vitest": "^3.2.7",
    "wrangler": "^4.80.0"
  }
}
