/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const RECOVERY_WALLET_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [172, 239, 138, 56, 173, 41, 182, 161],
);

export function getRecoveryWalletDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECOVERY_WALLET_DISCRIMINATOR,
  );
}

export type RecoveryWallet = {
  discriminator: ReadonlyUint8Array;
  phygitalToken: Address;
  /** Ed25519 pubkey authorized to recover the wallet. */
  recoveryWallet: Address;
  /** Account that paid init rent; close refunds this pubkey. */
  payer: Address;
  bump: number;
};

export type RecoveryWalletArgs = {
  phygitalToken: Address;
  /** Ed25519 pubkey authorized to recover the wallet. */
  recoveryWallet: Address;
  /** Account that paid init rent; close refunds this pubkey. */
  payer: Address;
  bump: number;
};

/** Gets the encoder for {@link RecoveryWalletArgs} account data. */
export function getRecoveryWalletEncoder(): FixedSizeEncoder<RecoveryWalletArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["phygitalToken", getAddressEncoder()],
      ["recoveryWallet", getAddressEncoder()],
      ["payer", getAddressEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RECOVERY_WALLET_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RecoveryWallet} account data. */
export function getRecoveryWalletDecoder(): FixedSizeDecoder<RecoveryWallet> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["phygitalToken", getAddressDecoder()],
    ["recoveryWallet", getAddressDecoder()],
    ["payer", getAddressDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link RecoveryWallet} account data. */
export function getRecoveryWalletCodec(): FixedSizeCodec<
  RecoveryWalletArgs,
  RecoveryWallet
> {
  return combineCodec(getRecoveryWalletEncoder(), getRecoveryWalletDecoder());
}

export function decodeRecoveryWallet<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RecoveryWallet, TAddress>;
export function decodeRecoveryWallet<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RecoveryWallet, TAddress>;
export function decodeRecoveryWallet<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<RecoveryWallet, TAddress> | MaybeAccount<RecoveryWallet, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRecoveryWalletDecoder(),
  );
}

export async function fetchRecoveryWallet<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RecoveryWallet, TAddress>> {
  const maybeAccount = await fetchMaybeRecoveryWallet(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRecoveryWallet<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RecoveryWallet, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRecoveryWallet(maybeAccount);
}

export async function fetchAllRecoveryWallet(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RecoveryWallet>[]> {
  const maybeAccounts = await fetchAllMaybeRecoveryWallet(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRecoveryWallet(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RecoveryWallet>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeRecoveryWallet(maybeAccount),
  );
}

export function getRecoveryWalletSize(): number {
  return 105;
}
