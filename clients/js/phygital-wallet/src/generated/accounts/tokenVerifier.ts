/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const TOKEN_VERIFIER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  52, 84, 111, 160, 81, 247, 144, 43,
]);

export function getTokenVerifierDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    TOKEN_VERIFIER_DISCRIMINATOR,
  );
}

export type TokenVerifier = {
  discriminator: ReadonlyUint8Array;
  phygitalToken: Address;
  verifier: Address;
  endpoint: string;
  /** Account that paid init rent; close refunds this pubkey. */
  payer: Address;
  bump: number;
};

export type TokenVerifierArgs = {
  phygitalToken: Address;
  verifier: Address;
  endpoint: string;
  /** Account that paid init rent; close refunds this pubkey. */
  payer: Address;
  bump: number;
};

/** Gets the encoder for {@link TokenVerifierArgs} account data. */
export function getTokenVerifierEncoder(): Encoder<TokenVerifierArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["phygitalToken", getAddressEncoder()],
      ["verifier", getAddressEncoder()],
      ["endpoint", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["payer", getAddressEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: TOKEN_VERIFIER_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link TokenVerifier} account data. */
export function getTokenVerifierDecoder(): Decoder<TokenVerifier> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["phygitalToken", getAddressDecoder()],
    ["verifier", getAddressDecoder()],
    ["endpoint", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["payer", getAddressDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link TokenVerifier} account data. */
export function getTokenVerifierCodec(): Codec<
  TokenVerifierArgs,
  TokenVerifier
> {
  return combineCodec(getTokenVerifierEncoder(), getTokenVerifierDecoder());
}

export function decodeTokenVerifier<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<TokenVerifier, TAddress>;
export function decodeTokenVerifier<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<TokenVerifier, TAddress>;
export function decodeTokenVerifier<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<TokenVerifier, TAddress> | MaybeAccount<TokenVerifier, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getTokenVerifierDecoder(),
  );
}

export async function fetchTokenVerifier<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<TokenVerifier, TAddress>> {
  const maybeAccount = await fetchMaybeTokenVerifier(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeTokenVerifier<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<TokenVerifier, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeTokenVerifier(maybeAccount);
}

export async function fetchAllTokenVerifier(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<TokenVerifier>[]> {
  const maybeAccounts = await fetchAllMaybeTokenVerifier(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeTokenVerifier(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<TokenVerifier>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeTokenVerifier(maybeAccount));
}
