/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findRecoveryWalletAccountPda, findWalletPda } from "../pdas/index.js";
import { PHYGITAL_WALLET_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  getCompactInstructionDecoder,
  getCompactInstructionEncoder,
  type CompactInstruction,
  type CompactInstructionArgs,
} from "../types/index.js";

export const RECOVERY_WALLET_EXECUTE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([91, 171, 164, 84, 112, 80, 230, 134]);

export function getRecoveryWalletExecuteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECOVERY_WALLET_EXECUTE_DISCRIMINATOR,
  );
}

export type RecoveryWalletExecuteInstruction<
  TProgram extends string = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
  TAccountRecoveryWallet extends string | AccountMeta<string> = string,
  TAccountPhygitalToken extends string | AccountMeta<string> = string,
  TAccountRecoveryWalletAccount extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRecoveryWallet extends string
        ? ReadonlySignerAccount<TAccountRecoveryWallet> &
            AccountSignerMeta<TAccountRecoveryWallet>
        : TAccountRecoveryWallet,
      TAccountPhygitalToken extends string
        ? WritableAccount<TAccountPhygitalToken>
        : TAccountPhygitalToken,
      TAccountRecoveryWalletAccount extends string
        ? ReadonlyAccount<TAccountRecoveryWalletAccount>
        : TAccountRecoveryWalletAccount,
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      ...TRemainingAccounts,
    ]
  >;

export type RecoveryWalletExecuteInstructionData = {
  discriminator: ReadonlyUint8Array;
  compactInstructions: Array<CompactInstruction>;
};

export type RecoveryWalletExecuteInstructionDataArgs = {
  compactInstructions: Array<CompactInstructionArgs>;
};

export function getRecoveryWalletExecuteInstructionDataEncoder(): Encoder<RecoveryWalletExecuteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["compactInstructions", getArrayEncoder(getCompactInstructionEncoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: RECOVERY_WALLET_EXECUTE_DISCRIMINATOR,
    }),
  );
}

export function getRecoveryWalletExecuteInstructionDataDecoder(): Decoder<RecoveryWalletExecuteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["compactInstructions", getArrayDecoder(getCompactInstructionDecoder())],
  ]);
}

export function getRecoveryWalletExecuteInstructionDataCodec(): Codec<
  RecoveryWalletExecuteInstructionDataArgs,
  RecoveryWalletExecuteInstructionData
> {
  return combineCodec(
    getRecoveryWalletExecuteInstructionDataEncoder(),
    getRecoveryWalletExecuteInstructionDataDecoder(),
  );
}

export type RecoveryWalletExecuteAsyncInput<
  TAccountRecoveryWallet extends string = string,
  TAccountPhygitalToken extends string = string,
  TAccountRecoveryWalletAccount extends string = string,
  TAccountWallet extends string = string,
> = {
  /** Must match `recovery_wallet_account.recovery_wallet`. */
  recoveryWallet: TransactionSigner<TAccountRecoveryWallet>;
  phygitalToken: Address<TAccountPhygitalToken>;
  recoveryWalletAccount?: Address<TAccountRecoveryWalletAccount>;
  wallet?: Address<TAccountWallet>;
  compactInstructions: RecoveryWalletExecuteInstructionDataArgs["compactInstructions"];
};

export async function getRecoveryWalletExecuteInstructionAsync<
  TAccountRecoveryWallet extends string,
  TAccountPhygitalToken extends string,
  TAccountRecoveryWalletAccount extends string,
  TAccountWallet extends string,
  TProgramAddress extends Address = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
>(
  input: RecoveryWalletExecuteAsyncInput<
    TAccountRecoveryWallet,
    TAccountPhygitalToken,
    TAccountRecoveryWalletAccount,
    TAccountWallet
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecoveryWalletExecuteInstruction<
    TProgramAddress,
    TAccountRecoveryWallet,
    TAccountPhygitalToken,
    TAccountRecoveryWalletAccount,
    TAccountWallet
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    recoveryWallet: { value: input.recoveryWallet ?? null, isWritable: false },
    phygitalToken: { value: input.phygitalToken ?? null, isWritable: true },
    recoveryWalletAccount: {
      value: input.recoveryWalletAccount ?? null,
      isWritable: false,
    },
    wallet: { value: input.wallet ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.recoveryWalletAccount.value) {
    accounts.recoveryWalletAccount.value = await findRecoveryWalletAccountPda({
      phygitalToken: getAddressFromResolvedInstructionAccount(
        "phygitalToken",
        accounts.phygitalToken.value,
      ),
    });
  }
  if (!accounts.wallet.value) {
    accounts.wallet.value = await findWalletPda({
      phygitalToken: getAddressFromResolvedInstructionAccount(
        "phygitalToken",
        accounts.phygitalToken.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("recoveryWallet", accounts.recoveryWallet),
      getAccountMeta("phygitalToken", accounts.phygitalToken),
      getAccountMeta("recoveryWalletAccount", accounts.recoveryWalletAccount),
      getAccountMeta("wallet", accounts.wallet),
    ],
    data: getRecoveryWalletExecuteInstructionDataEncoder().encode(
      args as RecoveryWalletExecuteInstructionDataArgs,
    ),
    programAddress,
  } as RecoveryWalletExecuteInstruction<
    TProgramAddress,
    TAccountRecoveryWallet,
    TAccountPhygitalToken,
    TAccountRecoveryWalletAccount,
    TAccountWallet
  >);
}

export type RecoveryWalletExecuteInput<
  TAccountRecoveryWallet extends string = string,
  TAccountPhygitalToken extends string = string,
  TAccountRecoveryWalletAccount extends string = string,
  TAccountWallet extends string = string,
> = {
  /** Must match `recovery_wallet_account.recovery_wallet`. */
  recoveryWallet: TransactionSigner<TAccountRecoveryWallet>;
  phygitalToken: Address<TAccountPhygitalToken>;
  recoveryWalletAccount: Address<TAccountRecoveryWalletAccount>;
  wallet: Address<TAccountWallet>;
  compactInstructions: RecoveryWalletExecuteInstructionDataArgs["compactInstructions"];
};

export function getRecoveryWalletExecuteInstruction<
  TAccountRecoveryWallet extends string,
  TAccountPhygitalToken extends string,
  TAccountRecoveryWalletAccount extends string,
  TAccountWallet extends string,
  TProgramAddress extends Address = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
>(
  input: RecoveryWalletExecuteInput<
    TAccountRecoveryWallet,
    TAccountPhygitalToken,
    TAccountRecoveryWalletAccount,
    TAccountWallet
  >,
  config?: { programAddress?: TProgramAddress },
): RecoveryWalletExecuteInstruction<
  TProgramAddress,
  TAccountRecoveryWallet,
  TAccountPhygitalToken,
  TAccountRecoveryWalletAccount,
  TAccountWallet
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    recoveryWallet: { value: input.recoveryWallet ?? null, isWritable: false },
    phygitalToken: { value: input.phygitalToken ?? null, isWritable: true },
    recoveryWalletAccount: {
      value: input.recoveryWalletAccount ?? null,
      isWritable: false,
    },
    wallet: { value: input.wallet ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("recoveryWallet", accounts.recoveryWallet),
      getAccountMeta("phygitalToken", accounts.phygitalToken),
      getAccountMeta("recoveryWalletAccount", accounts.recoveryWalletAccount),
      getAccountMeta("wallet", accounts.wallet),
    ],
    data: getRecoveryWalletExecuteInstructionDataEncoder().encode(
      args as RecoveryWalletExecuteInstructionDataArgs,
    ),
    programAddress,
  } as RecoveryWalletExecuteInstruction<
    TProgramAddress,
    TAccountRecoveryWallet,
    TAccountPhygitalToken,
    TAccountRecoveryWalletAccount,
    TAccountWallet
  >);
}

export type ParsedRecoveryWalletExecuteInstruction<
  TProgram extends string = typeof PHYGITAL_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Must match `recovery_wallet_account.recovery_wallet`. */
    recoveryWallet: TAccountMetas[0];
    phygitalToken: TAccountMetas[1];
    recoveryWalletAccount: TAccountMetas[2];
    wallet: TAccountMetas[3];
  };
  data: RecoveryWalletExecuteInstructionData;
};

export function parseRecoveryWalletExecuteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecoveryWalletExecuteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      recoveryWallet: getNextAccount(),
      phygitalToken: getNextAccount(),
      recoveryWalletAccount: getNextAccount(),
      wallet: getNextAccount(),
    },
    data: getRecoveryWalletExecuteInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
