/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithPayer,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type ExtendedClient,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfPlanAndSendFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/kit/program-client-core";
import {
  getAddAuthorityInstruction,
  getAuthorizeInstruction,
  getCreateSessionInstruction,
  getCreateWalletInstruction,
  getExecuteDeferredInstruction,
  getExecuteInstruction,
  getReclaimDeferredInstruction,
  getRemoveAuthorityInstruction,
  getRevokeSessionInstruction,
  getTransferOwnershipInstruction,
  parseAddAuthorityInstruction,
  parseAuthorizeInstruction,
  parseCreateSessionInstruction,
  parseCreateWalletInstruction,
  parseExecuteDeferredInstruction,
  parseExecuteInstruction,
  parseReclaimDeferredInstruction,
  parseRemoveAuthorityInstruction,
  parseRevokeSessionInstruction,
  parseTransferOwnershipInstruction,
  type AddAuthorityInput,
  type AuthorizeInput,
  type CreateSessionInput,
  type CreateWalletInput,
  type ExecuteDeferredInput,
  type ExecuteInput,
  type ParsedAddAuthorityInstruction,
  type ParsedAuthorizeInstruction,
  type ParsedCreateSessionInstruction,
  type ParsedCreateWalletInstruction,
  type ParsedExecuteDeferredInstruction,
  type ParsedExecuteInstruction,
  type ParsedReclaimDeferredInstruction,
  type ParsedRemoveAuthorityInstruction,
  type ParsedRevokeSessionInstruction,
  type ParsedTransferOwnershipInstruction,
  type ReclaimDeferredInput,
  type RemoveAuthorityInput,
  type RevokeSessionInput,
  type TransferOwnershipInput,
} from "../instructions";

export const LAZORKIT_PROGRAM_PROGRAM_ADDRESS =
  "LazorjRFNavitUaBu5m3WaNPjU1maipvSW2rZfAFAKi" as Address<"LazorjRFNavitUaBu5m3WaNPjU1maipvSW2rZfAFAKi">;

export enum LazorkitProgramInstruction {
  CreateWallet,
  AddAuthority,
  RemoveAuthority,
  TransferOwnership,
  Execute,
  CreateSession,
  Authorize,
  ExecuteDeferred,
  ReclaimDeferred,
  RevokeSession,
}

export function identifyLazorkitProgramInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): LazorkitProgramInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (containsBytes(data, getU8Encoder().encode(0), 0)) {
    return LazorkitProgramInstruction.CreateWallet;
  }
  if (containsBytes(data, getU8Encoder().encode(1), 0)) {
    return LazorkitProgramInstruction.AddAuthority;
  }
  if (containsBytes(data, getU8Encoder().encode(2), 0)) {
    return LazorkitProgramInstruction.RemoveAuthority;
  }
  if (containsBytes(data, getU8Encoder().encode(3), 0)) {
    return LazorkitProgramInstruction.TransferOwnership;
  }
  if (containsBytes(data, getU8Encoder().encode(4), 0)) {
    return LazorkitProgramInstruction.Execute;
  }
  if (containsBytes(data, getU8Encoder().encode(5), 0)) {
    return LazorkitProgramInstruction.CreateSession;
  }
  if (containsBytes(data, getU8Encoder().encode(6), 0)) {
    return LazorkitProgramInstruction.Authorize;
  }
  if (containsBytes(data, getU8Encoder().encode(7), 0)) {
    return LazorkitProgramInstruction.ExecuteDeferred;
  }
  if (containsBytes(data, getU8Encoder().encode(8), 0)) {
    return LazorkitProgramInstruction.ReclaimDeferred;
  }
  if (containsBytes(data, getU8Encoder().encode(9), 0)) {
    return LazorkitProgramInstruction.RevokeSession;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "lazorkitProgram" },
  );
}

export type ParsedLazorkitProgramInstruction<TProgram extends string = ""> =
  | ({
      instructionType: LazorkitProgramInstruction.CreateWallet;
    } & ParsedCreateWalletInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.AddAuthority;
    } & ParsedAddAuthorityInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.RemoveAuthority;
    } & ParsedRemoveAuthorityInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.TransferOwnership;
    } & ParsedTransferOwnershipInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.Execute;
    } & ParsedExecuteInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.CreateSession;
    } & ParsedCreateSessionInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.Authorize;
    } & ParsedAuthorizeInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.ExecuteDeferred;
    } & ParsedExecuteDeferredInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.ReclaimDeferred;
    } & ParsedReclaimDeferredInstruction<TProgram>)
  | ({
      instructionType: LazorkitProgramInstruction.RevokeSession;
    } & ParsedRevokeSessionInstruction<TProgram>);

export function parseLazorkitProgramInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedLazorkitProgramInstruction<TProgram> {
  const instructionType = identifyLazorkitProgramInstruction(instruction);
  switch (instructionType) {
    case LazorkitProgramInstruction.CreateWallet: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.CreateWallet,
        ...parseCreateWalletInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.AddAuthority: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.AddAuthority,
        ...parseAddAuthorityInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.RemoveAuthority: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.RemoveAuthority,
        ...parseRemoveAuthorityInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.TransferOwnership: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.TransferOwnership,
        ...parseTransferOwnershipInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.Execute: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.Execute,
        ...parseExecuteInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.CreateSession: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.CreateSession,
        ...parseCreateSessionInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.Authorize: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.Authorize,
        ...parseAuthorizeInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.ExecuteDeferred: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.ExecuteDeferred,
        ...parseExecuteDeferredInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.ReclaimDeferred: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.ReclaimDeferred,
        ...parseReclaimDeferredInstruction(instruction),
      };
    }
    case LazorkitProgramInstruction.RevokeSession: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: LazorkitProgramInstruction.RevokeSession,
        ...parseRevokeSessionInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "lazorkitProgram",
        },
      );
  }
}

export type LazorkitProgramPlugin = {
  instructions: LazorkitProgramPluginInstructions;
  identifyInstruction: typeof identifyLazorkitProgramInstruction;
  parseInstruction: typeof parseLazorkitProgramInstruction;
};

export type LazorkitProgramPluginInstructions = {
  createWallet: (
    input: MakeOptional<CreateWalletInput, "payer">,
  ) => ReturnType<typeof getCreateWalletInstruction> & SelfPlanAndSendFunctions;
  addAuthority: (
    input: MakeOptional<AddAuthorityInput, "payer">,
  ) => ReturnType<typeof getAddAuthorityInstruction> & SelfPlanAndSendFunctions;
  removeAuthority: (
    input: MakeOptional<RemoveAuthorityInput, "payer">,
  ) => ReturnType<typeof getRemoveAuthorityInstruction> &
    SelfPlanAndSendFunctions;
  transferOwnership: (
    input: MakeOptional<TransferOwnershipInput, "payer">,
  ) => ReturnType<typeof getTransferOwnershipInstruction> &
    SelfPlanAndSendFunctions;
  execute: (
    input: MakeOptional<ExecuteInput, "payer">,
  ) => ReturnType<typeof getExecuteInstruction> & SelfPlanAndSendFunctions;
  createSession: (
    input: MakeOptional<CreateSessionInput, "payer">,
  ) => ReturnType<typeof getCreateSessionInstruction> &
    SelfPlanAndSendFunctions;
  authorize: (
    input: MakeOptional<AuthorizeInput, "payer">,
  ) => ReturnType<typeof getAuthorizeInstruction> & SelfPlanAndSendFunctions;
  executeDeferred: (
    input: MakeOptional<ExecuteDeferredInput, "payer">,
  ) => ReturnType<typeof getExecuteDeferredInstruction> &
    SelfPlanAndSendFunctions;
  reclaimDeferred: (
    input: MakeOptional<ReclaimDeferredInput, "payer">,
  ) => ReturnType<typeof getReclaimDeferredInstruction> &
    SelfPlanAndSendFunctions;
  revokeSession: (
    input: MakeOptional<RevokeSessionInput, "payer">,
  ) => ReturnType<typeof getRevokeSessionInstruction> &
    SelfPlanAndSendFunctions;
};

export type LazorkitProgramPluginRequirements = ClientWithPayer &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function lazorkitProgramProgram() {
  return <T extends LazorkitProgramPluginRequirements>(
    client: T,
  ): ExtendedClient<T, { lazorkitProgram: LazorkitProgramPlugin }> => {
    return extendClient(client, {
      lazorkitProgram: <LazorkitProgramPlugin>{
        instructions: {
          createWallet: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCreateWalletInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          addAuthority: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getAddAuthorityInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          removeAuthority: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getRemoveAuthorityInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          transferOwnership: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getTransferOwnershipInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          execute: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getExecuteInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          createSession: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCreateSessionInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          authorize: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getAuthorizeInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          executeDeferred: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getExecuteDeferredInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          reclaimDeferred: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getReclaimDeferredInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
          revokeSession: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getRevokeSessionInstruction({
                ...input,
                payer: input.payer ?? client.payer,
              }),
            ),
        },
        identifyInstruction: identifyLazorkitProgramInstruction,
        parseInstruction: parseLazorkitProgramInstruction,
      },
    });
  };
}

type MakeOptional<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
