/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LAZORKIT_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const RECLAIM_DEFERRED_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([158, 3, 251, 8, 45, 151, 175, 46]);

export function getReclaimDeferredDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECLAIM_DEFERRED_DISCRIMINATOR,
  );
}

export type ReclaimDeferredInstruction<
  TProgram extends string = typeof LAZORKIT_PROGRAM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountDeferredExec extends string | AccountMeta<string> = string,
  TAccountRefundDestination extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? ReadonlySignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountDeferredExec extends string
        ? WritableAccount<TAccountDeferredExec>
        : TAccountDeferredExec,
      TAccountRefundDestination extends string
        ? WritableAccount<TAccountRefundDestination>
        : TAccountRefundDestination,
      ...TRemainingAccounts,
    ]
  >;

export type ReclaimDeferredInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ReclaimDeferredInstructionDataArgs = {};

export function getReclaimDeferredInstructionDataEncoder(): FixedSizeEncoder<ReclaimDeferredInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RECLAIM_DEFERRED_DISCRIMINATOR }),
  );
}

export function getReclaimDeferredInstructionDataDecoder(): FixedSizeDecoder<ReclaimDeferredInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getReclaimDeferredInstructionDataCodec(): FixedSizeCodec<
  ReclaimDeferredInstructionDataArgs,
  ReclaimDeferredInstructionData
> {
  return combineCodec(
    getReclaimDeferredInstructionDataEncoder(),
    getReclaimDeferredInstructionDataDecoder(),
  );
}

export type ReclaimDeferredInput<
  TAccountPayer extends string = string,
  TAccountDeferredExec extends string = string,
  TAccountRefundDestination extends string = string,
> = {
  /** Original payer (must match stored payer) */
  payer: TransactionSigner<TAccountPayer>;
  /** Expired DeferredExec PDA to close */
  deferredExec: Address<TAccountDeferredExec>;
  /** Account to receive rent refund */
  refundDestination: Address<TAccountRefundDestination>;
};

export function getReclaimDeferredInstruction<
  TAccountPayer extends string,
  TAccountDeferredExec extends string,
  TAccountRefundDestination extends string,
  TProgramAddress extends Address = typeof LAZORKIT_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ReclaimDeferredInput<
    TAccountPayer,
    TAccountDeferredExec,
    TAccountRefundDestination
  >,
  config?: { programAddress?: TProgramAddress },
): ReclaimDeferredInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountDeferredExec,
  TAccountRefundDestination
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LAZORKIT_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: false },
    deferredExec: { value: input.deferredExec ?? null, isWritable: true },
    refundDestination: {
      value: input.refundDestination ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("deferredExec", accounts.deferredExec),
      getAccountMeta("refundDestination", accounts.refundDestination),
    ],
    data: getReclaimDeferredInstructionDataEncoder().encode({}),
    programAddress,
  } as ReclaimDeferredInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountDeferredExec,
    TAccountRefundDestination
  >);
}

export type ParsedReclaimDeferredInstruction<
  TProgram extends string = typeof LAZORKIT_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Original payer (must match stored payer) */
    payer: TAccountMetas[0];
    /** Expired DeferredExec PDA to close */
    deferredExec: TAccountMetas[1];
    /** Account to receive rent refund */
    refundDestination: TAccountMetas[2];
  };
  data: ReclaimDeferredInstructionData;
};

export function parseReclaimDeferredInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedReclaimDeferredInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      deferredExec: getNextAccount(),
      refundDestination: getNextAccount(),
    },
    data: getReclaimDeferredInstructionDataDecoder().decode(instruction.data),
  };
}
