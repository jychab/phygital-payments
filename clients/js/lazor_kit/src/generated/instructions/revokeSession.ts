/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { LAZORKIT_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const REVOKE_SESSION_DISCRIMINATOR = 9;

export function getRevokeSessionDiscriminatorBytes(): ReadonlyUint8Array {
  return getU8Encoder().encode(REVOKE_SESSION_DISCRIMINATOR);
}

export type RevokeSessionInstruction<
  TProgram extends string = typeof LAZORKIT_PROGRAM_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountAdminAuthority extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TAccountRefundDestination extends string | AccountMeta<string> = string,
  TAccountAuthExtra extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountWallet extends string
        ? ReadonlyAccount<TAccountWallet>
        : TAccountWallet,
      TAccountAdminAuthority extends string
        ? WritableAccount<TAccountAdminAuthority>
        : TAccountAdminAuthority,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      TAccountRefundDestination extends string
        ? WritableAccount<TAccountRefundDestination>
        : TAccountRefundDestination,
      TAccountAuthExtra extends string
        ? ReadonlyAccount<TAccountAuthExtra>
        : TAccountAuthExtra,
      ...TRemainingAccounts,
    ]
  >;

export type RevokeSessionInstructionData = { discriminator: number };

export type RevokeSessionInstructionDataArgs = {};

export function getRevokeSessionInstructionDataEncoder(): FixedSizeEncoder<RevokeSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", getU8Encoder()]]),
    (value) => ({ ...value, discriminator: REVOKE_SESSION_DISCRIMINATOR }),
  );
}

export function getRevokeSessionInstructionDataDecoder(): FixedSizeDecoder<RevokeSessionInstructionData> {
  return getStructDecoder([["discriminator", getU8Decoder()]]);
}

export function getRevokeSessionInstructionDataCodec(): FixedSizeCodec<
  RevokeSessionInstructionDataArgs,
  RevokeSessionInstructionData
> {
  return combineCodec(
    getRevokeSessionInstructionDataEncoder(),
    getRevokeSessionInstructionDataDecoder(),
  );
}

export type RevokeSessionInput<
  TAccountPayer extends string = string,
  TAccountWallet extends string = string,
  TAccountAdminAuthority extends string = string,
  TAccountSession extends string = string,
  TAccountRefundDestination extends string = string,
  TAccountAuthExtra extends string = string,
> = {
  /** Transaction payer */
  payer: TransactionSigner<TAccountPayer>;
  /** Wallet PDA */
  wallet: Address<TAccountWallet>;
  /** Owner/Admin authority PDA (counter incremented for Secp256r1) */
  adminAuthority: Address<TAccountAdminAuthority>;
  /** Session PDA to revoke */
  session: Address<TAccountSession>;
  /** Account to receive rent refund */
  refundDestination: Address<TAccountRefundDestination>;
  /** Ed25519: signer keypair | Secp256r1: sysvar_instructions */
  authExtra?: Address<TAccountAuthExtra>;
};

export function getRevokeSessionInstruction<
  TAccountPayer extends string,
  TAccountWallet extends string,
  TAccountAdminAuthority extends string,
  TAccountSession extends string,
  TAccountRefundDestination extends string,
  TAccountAuthExtra extends string,
  TProgramAddress extends Address = typeof LAZORKIT_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RevokeSessionInput<
    TAccountPayer,
    TAccountWallet,
    TAccountAdminAuthority,
    TAccountSession,
    TAccountRefundDestination,
    TAccountAuthExtra
  >,
  config?: { programAddress?: TProgramAddress },
): RevokeSessionInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountWallet,
  TAccountAdminAuthority,
  TAccountSession,
  TAccountRefundDestination,
  TAccountAuthExtra
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? LAZORKIT_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    wallet: { value: input.wallet ?? null, isWritable: false },
    adminAuthority: { value: input.adminAuthority ?? null, isWritable: true },
    session: { value: input.session ?? null, isWritable: true },
    refundDestination: {
      value: input.refundDestination ?? null,
      isWritable: true,
    },
    authExtra: { value: input.authExtra ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("wallet", accounts.wallet),
      getAccountMeta("adminAuthority", accounts.adminAuthority),
      getAccountMeta("session", accounts.session),
      getAccountMeta("refundDestination", accounts.refundDestination),
      getAccountMeta("authExtra", accounts.authExtra),
    ],
    data: getRevokeSessionInstructionDataEncoder().encode({}),
    programAddress,
  } as RevokeSessionInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountWallet,
    TAccountAdminAuthority,
    TAccountSession,
    TAccountRefundDestination,
    TAccountAuthExtra
  >);
}

export type ParsedRevokeSessionInstruction<
  TProgram extends string = typeof LAZORKIT_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Transaction payer */
    payer: TAccountMetas[0];
    /** Wallet PDA */
    wallet: TAccountMetas[1];
    /** Owner/Admin authority PDA (counter incremented for Secp256r1) */
    adminAuthority: TAccountMetas[2];
    /** Session PDA to revoke */
    session: TAccountMetas[3];
    /** Account to receive rent refund */
    refundDestination: TAccountMetas[4];
    /** Ed25519: signer keypair | Secp256r1: sysvar_instructions */
    authExtra?: TAccountMetas[5] | undefined;
  };
  data: RevokeSessionInstructionData;
};

export function parseRevokeSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRevokeSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === LAZORKIT_PROGRAM_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      wallet: getNextAccount(),
      adminAuthority: getNextAccount(),
      session: getNextAccount(),
      refundDestination: getNextAccount(),
      authExtra: getNextOptionalAccount(),
    },
    data: getRevokeSessionInstructionDataDecoder().decode(instruction.data),
  };
}
