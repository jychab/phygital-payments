/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findConfigPda } from "../pdas/index.js";
import { PHYGITAL_PAYMENTS_PROGRAM_ADDRESS } from "../programs/index.js";

export const REMOVE_VERIFIER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [179, 9, 132, 183, 233, 23, 172, 111],
);

export function getRemoveVerifierDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REMOVE_VERIFIER_DISCRIMINATOR,
  );
}

export type RemoveVerifierInstruction<
  TProgram extends string = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
  TAccountAdmin extends string | AccountMeta<string> =
    "G6kBnedts6uAivtY72ToaFHBs1UVbT9udiXmQZgMEjoF",
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveVerifierInstructionData = {
  discriminator: ReadonlyUint8Array;
  verifier: Address;
};

export type RemoveVerifierInstructionDataArgs = { verifier: Address };

export function getRemoveVerifierInstructionDataEncoder(): FixedSizeEncoder<RemoveVerifierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["verifier", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REMOVE_VERIFIER_DISCRIMINATOR }),
  );
}

export function getRemoveVerifierInstructionDataDecoder(): FixedSizeDecoder<RemoveVerifierInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["verifier", getAddressDecoder()],
  ]);
}

export function getRemoveVerifierInstructionDataCodec(): FixedSizeCodec<
  RemoveVerifierInstructionDataArgs,
  RemoveVerifierInstructionData
> {
  return combineCodec(
    getRemoveVerifierInstructionDataEncoder(),
    getRemoveVerifierInstructionDataDecoder(),
  );
}

export type RemoveVerifierAsyncInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin?: TransactionSigner<TAccountAdmin>;
  config?: Address<TAccountConfig>;
  verifier: RemoveVerifierInstructionDataArgs["verifier"];
};

export async function getRemoveVerifierInstructionAsync<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
>(
  input: RemoveVerifierAsyncInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RemoveVerifierInstruction<TProgramAddress, TAccountAdmin, TAccountConfig>
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_PAYMENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.admin.value) {
    accounts.admin.value =
      "G6kBnedts6uAivtY72ToaFHBs1UVbT9udiXmQZgMEjoF" as Address<"G6kBnedts6uAivtY72ToaFHBs1UVbT9udiXmQZgMEjoF">;
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getRemoveVerifierInstructionDataEncoder().encode(
      args as RemoveVerifierInstructionDataArgs,
    ),
    programAddress,
  } as RemoveVerifierInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type RemoveVerifierInput<
  TAccountAdmin extends string = string,
  TAccountConfig extends string = string,
> = {
  admin?: TransactionSigner<TAccountAdmin>;
  config: Address<TAccountConfig>;
  verifier: RemoveVerifierInstructionDataArgs["verifier"];
};

export function getRemoveVerifierInstruction<
  TAccountAdmin extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
>(
  input: RemoveVerifierInput<TAccountAdmin, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): RemoveVerifierInstruction<TProgramAddress, TAccountAdmin, TAccountConfig> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_PAYMENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    admin: { value: input.admin ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.admin.value) {
    accounts.admin.value =
      "G6kBnedts6uAivtY72ToaFHBs1UVbT9udiXmQZgMEjoF" as Address<"G6kBnedts6uAivtY72ToaFHBs1UVbT9udiXmQZgMEjoF">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("admin", accounts.admin),
      getAccountMeta("config", accounts.config),
    ],
    data: getRemoveVerifierInstructionDataEncoder().encode(
      args as RemoveVerifierInstructionDataArgs,
    ),
    programAddress,
  } as RemoveVerifierInstruction<
    TProgramAddress,
    TAccountAdmin,
    TAccountConfig
  >);
}

export type ParsedRemoveVerifierInstruction<
  TProgram extends string = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    admin: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: RemoveVerifierInstructionData;
};

export function parseRemoveVerifierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveVerifierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { admin: getNextAccount(), config: getNextAccount() },
    data: getRemoveVerifierInstructionDataDecoder().decode(instruction.data),
  };
}
