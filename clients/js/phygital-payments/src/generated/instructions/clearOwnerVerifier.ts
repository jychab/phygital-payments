/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findOwnerVerifierPda } from "../pdas/index.js";
import { PHYGITAL_PAYMENTS_PROGRAM_ADDRESS } from "../programs/index.js";

export const CLEAR_OWNER_VERIFIER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([22, 210, 50, 56, 238, 126, 242, 84]);

export function getClearOwnerVerifierDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLEAR_OWNER_VERIFIER_DISCRIMINATOR,
  );
}

export type ClearOwnerVerifierInstruction<
  TProgram extends string = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountOwnerVerifier extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountOwnerVerifier extends string
        ? WritableAccount<TAccountOwnerVerifier>
        : TAccountOwnerVerifier,
      ...TRemainingAccounts,
    ]
  >;

export type ClearOwnerVerifierInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClearOwnerVerifierInstructionDataArgs = {};

export function getClearOwnerVerifierInstructionDataEncoder(): FixedSizeEncoder<ClearOwnerVerifierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: CLEAR_OWNER_VERIFIER_DISCRIMINATOR,
    }),
  );
}

export function getClearOwnerVerifierInstructionDataDecoder(): FixedSizeDecoder<ClearOwnerVerifierInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClearOwnerVerifierInstructionDataCodec(): FixedSizeCodec<
  ClearOwnerVerifierInstructionDataArgs,
  ClearOwnerVerifierInstructionData
> {
  return combineCodec(
    getClearOwnerVerifierInstructionDataEncoder(),
    getClearOwnerVerifierInstructionDataDecoder(),
  );
}

export type ClearOwnerVerifierAsyncInput<
  TAccountOwner extends string = string,
  TAccountOwnerVerifier extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  ownerVerifier?: Address<TAccountOwnerVerifier>;
};

export async function getClearOwnerVerifierInstructionAsync<
  TAccountOwner extends string,
  TAccountOwnerVerifier extends string,
  TProgramAddress extends Address = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
>(
  input: ClearOwnerVerifierAsyncInput<TAccountOwner, TAccountOwnerVerifier>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClearOwnerVerifierInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountOwnerVerifier
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_PAYMENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    ownerVerifier: { value: input.ownerVerifier ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.ownerVerifier.value) {
    accounts.ownerVerifier.value = await findOwnerVerifierPda({
      owner: getAddressFromResolvedInstructionAccount(
        "owner",
        accounts.owner.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("ownerVerifier", accounts.ownerVerifier),
    ],
    data: getClearOwnerVerifierInstructionDataEncoder().encode({}),
    programAddress,
  } as ClearOwnerVerifierInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountOwnerVerifier
  >);
}

export type ClearOwnerVerifierInput<
  TAccountOwner extends string = string,
  TAccountOwnerVerifier extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  ownerVerifier: Address<TAccountOwnerVerifier>;
};

export function getClearOwnerVerifierInstruction<
  TAccountOwner extends string,
  TAccountOwnerVerifier extends string,
  TProgramAddress extends Address = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
>(
  input: ClearOwnerVerifierInput<TAccountOwner, TAccountOwnerVerifier>,
  config?: { programAddress?: TProgramAddress },
): ClearOwnerVerifierInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountOwnerVerifier
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PHYGITAL_PAYMENTS_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    ownerVerifier: { value: input.ownerVerifier ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("ownerVerifier", accounts.ownerVerifier),
    ],
    data: getClearOwnerVerifierInstructionDataEncoder().encode({}),
    programAddress,
  } as ClearOwnerVerifierInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountOwnerVerifier
  >);
}

export type ParsedClearOwnerVerifierInstruction<
  TProgram extends string = typeof PHYGITAL_PAYMENTS_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    ownerVerifier: TAccountMetas[1];
  };
  data: ClearOwnerVerifierInstructionData;
};

export function parseClearOwnerVerifierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClearOwnerVerifierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), ownerVerifier: getNextAccount() },
    data: getClearOwnerVerifierInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
